http:
  address: 0.0.0.0:3000

dns:
  bind_hosts:
    - 0.0.0.0
  port: 53
  ratelimit: 0
  refuse_any: true
  upstream_dns:
    - tcp://172.30.0.20:5335
  upstream_mode: parallel
  use_private_ptr_resolvers: true
  local_ptr_upstreams:
    - tcp://172.30.0.20:5335
